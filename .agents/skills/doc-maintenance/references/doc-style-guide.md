# Documentation Style Guide

- Keep docs concrete and reviewable.
- Prefer current commands over prose descriptions.
- Keep examples deterministic and free of real credentials.
- Use `no-run` on code fences that are illustrative or environment-dependent.
- Put generated content inside generated markers.
- Keep assumptions visible instead of writing guesses as facts.

