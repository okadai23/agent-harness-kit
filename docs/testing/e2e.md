# E2E Testing

For browser apps, prefer Playwright for user-visible flows.

Good E2E tests:

- assert behavior rather than implementation details
- use deterministic dummy data
- avoid real external services
- capture screenshots or traces when debugging UI regressions

