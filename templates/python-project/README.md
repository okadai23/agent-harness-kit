# Your Project

A short description of your Python project.

## Development

Install the project in editable mode:

```sh
python -m pip install -e ".[dev]"
```

Run the default checks:

```sh
nox -s lint
nox -s typecheck
nox -s tests
```
